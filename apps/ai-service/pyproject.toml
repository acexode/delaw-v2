[project]
name = "delaw-ai-service"
version = "1.0.0"
description = "DeLaw AI service — RAG pipeline, embeddings, and LLM orchestration."
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.110.0",
    "uvicorn[standard]>=0.30.0",
]

[tool.ruff]
line-length = 100
target-version = "py312"
