export default function LoginPage() {
  return (
    <div className="w-full max-w-[420px] rounded-[18px] border border-line bg-bg-800 p-9 shadow-[0_24px_70px_rgba(0,0,0,.45)]">
      <h1 className="mb-2 font-serif text-2xl font-bold text-text-cream">
        Welcome back
      </h1>
      <p className="text-sm text-text-muted">
        Login is scaffolded — the auth flow is built in a later sprint.
      </p>
    </div>
  );
}
